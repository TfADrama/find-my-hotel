import { VERY_LOW, LOW, MEDIUM, HIGH, VERY_HIGH } from './strings';

/**
 * Creates an array with all the user rating classifications
 */
export const RANKINGS_ARRAY = [VERY_LOW, LOW, MEDIUM, HIGH, VERY_HIGH];
